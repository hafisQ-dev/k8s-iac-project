* Amazon Elastic Compute Cloud(User Guide):>Documentation>Amazon EC2>User Guide>Create a key pair for your Amazon EC2 instanceas
* HashicorpDeveloper>Terraform>Documentation>Configuration Language>Variables>Use Variable (for variables.tf)
varables.tf: 1>aws_region2>instance_type3>key_name4>node_count
forexample: variable "degisken_adi" {
  type        = string
  default     = "varsayilan_deger"
  description = "Ne ise yaradigi"
}
Terraform Registry:>
structure of main terraform file: 1.Provider(aws_provider and aws_region); 2.Virtual Network Layer(VPC(virtual private cloud),subnet,internet gateway,route table and route table assoc.) 3.Security Group(ssh and ingress and egress ports) 4.Server and Connections(AMI and EC2 Instances)
AWS Structure: 
[ AWS BULUT (eu-west-2) ]
 └── VPC (10.0.0.0/16) - Sana özel izole sanal veri merkezi  ###(cidr:Classless Inter-Domain Routing)
      ├── Internet Gateway - Sanal veri merkezinin internet vanası
      └── Subnet (10.0.0.0/24) - Sunucuların dizileceği mahal/blok
           ├── Security Group - Sunucuların önündeki firewall (Kapı Portları)
           ├── EC2: Master Node (k8s-master) - Kontrol düzlemi
           ├── EC2: Worker Node 1 (k8s-worker-1) - İş yükü sunucusu
           └── EC2: Worker Node 2 (k8s-worker-2) - İş yükü sunucusu
source types: resources(0'dan kaynak oluşturma(server,vpc,firewall rules et.)); data sources(var olana sorgu atma(aws_ami-ubnutu imajı çekme));list resource.
terraform.tfvars structure >>>> db_password = "SuperSecretPassword123!"
Meta-Argument: count, for_each, depends_on, provider, lifecycle  (terraform hashicorp )
Özet: Parametre Seçim Mantığı
1.İşletim Sistemi Ne Olacak? -> ami
2.Donanım Ne Olacak? -> instance_type
3.Nerede/Hangi Ağda Çalışacak? -> subnet_id
4.Hangi Güvenlik Duvarı Koruyacak? -> vpc_security_group_id
5.İçine Nasıl Bağlanacağım? -> key_name
6.Kaç Tane Açılacak? -> count
***## ANSIBLE ##***
DOCS:
* Ansible Community Documentation > Ansible Configuration Settings
ansible configuration file:> ansible-config init --disabled > ansible.cfg.example (example file)  ($~ansible-config view)
[defaults]: inventory= inventory.ini (you have hosts in you main file path) ; remote_user= ubuntu (U are using (ami); host_key_checking=False (y/N)  (they are necessary changing features)
ansible.cfg: >>> 1.Where is my inventory list;2.Which users and keys will I use to connect Inventory;3. Will we being asked SSH confirming(y/N); 4.How tod sudo (authority) operations: [privilege_escalation] > become = True | become_method = sudo | become_user = root | become_ask_pass = False  |
ansible inventory file: > 
[group_name]
server_name ansible_host=<IP adres(you can get from terraform outputs>
[group_cluster:children]
server names
[group_cluster:vars]
ansible_user=ubuntu or another server name
ansible_ssh_private_key_file=
ansible_python_interpreter=
$~ansible all -m ping (for confirming ssh comnections and authority for all servers)
##** Ansible Playbook **##
# swap(takas alanı) operations: fallocate ➔ chmod ➔ mkswap ➔ swapon ➔ fstab edition.
1.Creating empty file on disk: fallocate -l 2G /swapfile
2.Root authority for swap file: sudo chmod 600 /swapfile  
   read=4,write=2,execution=1, nothing=0.   user>group>other (0600) owner=6(r+w), group and other = 0(nothing)
   example with symbolic number >>> # Sahibe çalıştırma izni ekle : chmod u+x betik.sh
3.Returning file to swap form: sudo mkswap /swapfile
4.Activate swap field: swapon /swapfile
5.Altering swap file permanently to /etcd/fstab:  /swapfile none swap sw 0 0 (src,path, filesystem type, swap options (as sw,ntfs.xsf),state)
# Kubernetes Kernel Modules:
  1. overlay: Linux Kernel'ının katmanlı dosya sistemi (OverlayFS) sürücüsüdür. ($~sudo modprobe overlay)
  2. br_netfilter: Linux köprü (Bridge) ağından geçen paketlerin, işletim sisteminin iptables güvenlik duvarı ve paket yönlendirme kuralları tarafından taranmasını sağlayan sürücüdür.($~sudo modprobe br_netfilter)  [Yapılmazsa >>> CNI (Calico/Flannel) eklentileri ağ tüneli oluşturamaz ve Pod'lara IP dağıtamazdı.]
*changed_when: false (Ansible İdempotency İnce Ayarı)
# IP forwarding by sysctl
  net.ipv4.ip_forward = 1  (Router)  IP forwarding izni  [Yapılmasza >>> Node-1'deki Pod, Node-2'deki Pod'a veri gönderemezdi. Ağ paketleri sunucu sınırında takılırdı.]
  net.bridge.bridge-nf-call-iptables = 1  [Yapılmazsa>>> Kube-Proxy ve Kubernetes Service yönlendirmeleri çalışmazdı. Servis IP'lerine atılan istekler hedefe ulaşmazdı.]
  net.bridge.bridge-nf-call-ip6tables = 1  (iptables veya IPVS kurallarını kullana bilmesi için)
General Network basics.
IPv4 (Internet Protocol Version 4):  192.168.1.50
IPv6 (Internet Protocol Version 6):  2001:0db8:85a3:0000:0000:8a2e:0370:7334
IP Forwarding: kendisine gelen bir ağ paketini, kendi üzerinde tutmayıp başka bir cihaza veya ağ arayüzüne (interface) aktarması işlemidir.
Network Bridge (Ağ Köprüsü/Sanal Anahtar / Switch) ve IPTables (Linux Güvenlik Duvarı & Trafik Yonlendiricisi)
# Kubernetes basic tools and Container Runtime 
Kubernetes Documentation > Getting Started > Installing kubeadm (kubeadm is a CLI) (for the 1st,2nd and 5th and 6th steps)
Docker Docs > Install Docker Engine on Ubuntu (for the 3rd and 4th steps)
* GPG (GNU Privacy Guard) Key
* We have to use ansible.builtin.deb822_repository module instead of ansible.builtin.get_repository
1. APT dependencies and REPO Preparation 
sudo apt-get install -y apt-transport-https ca-certificates curl gpg
2. MKDIR keyrings folder
3. Installing GPG key of Docker/Containerd 
4. Adding docker APT repo to the system
suites: "{{ ansible_distribution_release }}"  instead of $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
I got this information from terraform main.tf doc (server architecture == amd64, by the way)
5. Installing GPG key of Kubernetes
6. Adding APT repo to the system
-------------------------------------------------------
# Getting started to containerd
7.  Downloading containerd packets
8.  Creating containerd config folder
9.  And prodact containerd configuration and activate systemCgroup setting
10. Inıtialization and Do enable containerd service
------------------------------------------------------
11. Downloading kebernetes packets
12. Stop automatic update of Kubernetes packets
13. Do enable kubelet service
------------------------------------------------------
------Troubleshooting------



